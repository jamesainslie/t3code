node_modules
.bun
.turbo
.DS_Store
*.log
*.tsbuildinfo
apps/*/dist
.astro
packages/*/dist
.env
.env.local
build/
.logs/
release/
release-mock/
.t3
.idea/
apps/web/.playwright
apps/web/playwright-report
apps/web/src/components/__screenshots__
.vitest-*
__screenshots__/
.tanstack
squashfs-root/
.worktrees/
dist/

# Stray Vite chunks accidentally written to the repo root by misconfigured
# build commands. The intended outputs live under apps/*/dist/ which is
# already covered above.
/index-*.js
/assets/

# Claude/agent worktree metadata (per-agent scratch directories created by
# the worktrunk skill).
.claude/worktrees/

# Playwright MCP DOM snapshots — generated test artifacts, not source.
.playwright-mcp/
